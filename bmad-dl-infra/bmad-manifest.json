{
  "module-code": "bmad-dl-lifecycle",
  "capabilities": [
    {
      "name": "infra",
      "menu-code": "DLI2",
      "description": "Builds all project infrastructure (data pipeline, training loop, experiment tracking, evaluation harness, inference API) before experiments begin. Handles INF-* tasks. Infrastructure is done when smoke test passes with dummy data.",
      "supports-headless": true,
      "phase-name": "5-infra",
      "after": ["bmad-dl-techspec"],
      "before": ["bmad-dl-experiment"],
      "is-required": true,
      "output-location": "{output_folder}/implementation"
    }
  ]
}
